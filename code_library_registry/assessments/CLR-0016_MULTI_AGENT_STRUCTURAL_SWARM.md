# CLR-0016 — Multi-Agent Structural Intelligence / Swarm

## Assessment
Very high relevance. This is the first artifact in the recent sequence to test **composition of multiple structural systems in a shared environment** rather than only transfer or operand substitution.

Each agent nominally instantiates:

S(t) = A + sum_i(sum_n F_n(R_i)) + feedback-memory

and receives local neighbor and target context.

## Architectural significance
The important new abstraction is:

individual structural system + local perception + inter-agent coupling -> collective dynamics.

This is directly relevant to Echo subagents, lobby agents, glyph-instantiated agents, NVEs, and any future multi-agent environment.

A useful higher-order form is:

S_j(t) = A_j + R_j(t) + M_j(t) + sum_k C_jk(t)

where j identifies an agent and C_jk is an explicit coupling term between agents.

That is closer to the later Echo discrete family containing an explicit coupling term C_k than pretending all social interaction is merely feedback.

## Critical implementation issue: rays are not genuinely directional operators
Inside compute_fractal_rays(), each ray influences target_align = dot(ray,target_dir), but the accumulated target contribution is target_align * target_dir rather than a contribution along the ray itself.

Neighbor contributions are also added as common vectors independent of the ray direction.

Consequently, the eight ray channels are mostly repeated copies of the same social-force vectors with different target-alignment scalar weights. They are not eight independently acting directional operands.

A stronger implementation would project each environmental force onto each ray, accumulate a scalar activation for that ray, then reconstruct the action:

g_i = recursive_response(R_i, context)
V = sum_i g_i R_i.

This would make the rays operationally meaningful.

## Anchor issue
self.anchor is initialized to the initial position and described as identity, but is never used in action or state calculation.

Therefore the implemented dynamics do not literally compute the displayed S(t). If anchor represents identity rather than position, it should be typed separately from spatial state.

## Feedback issue
The comments say agents interact through feedback fields, but compute_collective_feedback() only reads the agent's own historical position/density. Other agents affect the current ray calculation through neighbor perception; they do not directly contribute memories or shared feedback fields.

So the implementation has local social coupling plus self-memory, not genuinely shared feedback fields.

This distinction matters for Echo.

## Emergence claim
Improved cohesion and target tracking can demonstrate collective behavior of the programmed controller, but the threshold rule does not establish collective intelligence.

The agents are explicitly given:
- target attraction,
- close-range repulsion,
- medium-range velocity alignment,
- long-range cohesion.

These are classic hand-designed ingredients capable of producing flocking-like organization. Any emergence claim should therefore distinguish behavior generated by local rules from newly learned collective structure.

The plotted "emergence" score is also constructed after the run by min-max normalizing cohesion and target distance over the same trajectory and applying an arbitrary 0.7 threshold.

Treat this as a descriptive visualization, not an independent emergence detector.

## Experimental issues
- No random seed; runs are not reproducible.
- Agents update sequentially in-place. Later agents in a timestep perceive some neighbors after they have already moved, creating order-dependent/asynchronous dynamics.
- Alignment includes self-pairs and all ordered pairs; this inflates/reweights the statistic.
- Minimum pair distance is calculated but not incorporated into the final emergence criterion.
- Boundaries clip positions, potentially creating edge accumulation.
- density = neighbor_count/10 can exceed the implied normalized range when more than 10 neighbors are perceived.
- interaction_history exists but is unused.
- Sample-agent visualization assumes IDs 0,5,10,15 and would fail for smaller swarms.

## Recommended experiment architecture
Use synchronous updates:
1. snapshot all agent states,
2. every agent perceives the same snapshot,
3. compute proposed actions,
4. commit all updates together.

Then run ablations:
- target only,
- target + repulsion,
- target + repulsion + alignment,
- no memory,
- self memory,
- explicit shared/inter-agent memory,
- full structural rays.

Compare many seeded runs.

## Echo relevance
This artifact suggests that multi-agent Echo should expose coupling explicitly rather than hide it inside a generic feedback term.

Candidate state family:

S_j,k = A_j(S_j,k-1) + F_j(U_j,k) + M_j(S_j,<k) + sum_l C_jl,k + epsilon_j,k.

This makes:
- identity/local state,
- environmental input,
- personal memory,
- inter-agent coupling,
- residual/noise

separately auditable.

## Relationship to the recent sequence
CLR-0013: scalar transfer/memory.
CLR-0014: vector geometry and translated-pattern transfer.
CLR-0015: operand-domain substitution into oscillatory dynamics.
CLR-0016: compositional closure — multiple structural systems interacting.

That sequence is increasingly useful as a test suite for what is invariant in the core formula.

## Disposition
Candidate for a corrected multi-agent benchmark. Do not record the historical threshold result as proof of collective intelligence. Preserve the stronger architectural result: the core formula can be investigated at both individual-agent and coupled-system levels.
